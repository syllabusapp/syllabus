mutation ResetPassword(
  $email: String!
  $newPassword: String!
  $resetToken: String!
) {
  resetPassword(
    email: $email
    newPassword: $newPassword
    resetToken: $resetToken
  ) {
    id
  }
}
