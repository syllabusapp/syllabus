// import {build, fake} from 'test-data-bot';

// const userBuilder = build('User').fields({
//   username: fake((f: any) => f.internet.email()),
//   password: fake((f: any) => f.internet.password()),
// });

// export {userBuilder};
